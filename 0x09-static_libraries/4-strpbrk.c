#include "main.h"

/**
 * _strpbrk - searches a string for set of bytes
 * @s: string
 * @accept: set of bytes
 * Return: pointer to the bytes in s that matches accept,NULL if not found
 */
char *_strpbrk(char *s, char *accept)
{
}
